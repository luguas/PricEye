{
  "name": "priceye-backend",
  "version": "1.0.0",
  "description": "Serveur backend pour l'application Pricing IA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "python3 -m pip install --user -r requirements.txt || pip3 install --user -r requirements.txt || echo 'Python dependencies installation skipped (Python may not be available)'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.13.1",
    "cors": "^2.8.5",
    "date-fns": "^2.30.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.1",
    "node-cron": "^3.0.3",
    "openai": "^4.20.0",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
